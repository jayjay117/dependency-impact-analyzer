{
  "name": "project-dependency-impact-analyzer",
  "private": true,
  "version": "1.0.0",
  "description": "Project Dependency & Impact Analyzer backed by CognoDB (managed graph database).",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build:frontend && npm run build:function",
    "build:frontend": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "build:function": "esbuild netlify/functions-src/api.js --bundle --platform=node --format=cjs --outfile=netlify/functions/api.cjs",
    "seed": "npm run seed --prefix backend",
    "start": "npm start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
