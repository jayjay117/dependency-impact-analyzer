# Screenshots & demo assets

Place the following files here for the submission README:

- `screenshot-browse.png` — **Browse dependencies** view: the dependency graph on the left,
  the "Direct dependencies" panel on the right.
- `screenshot-impact.png` — **Impact analysis** view: blast radius graph with the
  "affected components grouped by team" panel.

Also record a short (1–2 min) screen recording that walks through:

1. Search for a project/library/service.
2. Open its dependency tree (Browse view).
3. Switch to **Impact analysis** and show the affected teams.

Attach the recording link in your submission email.
